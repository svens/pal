See README.md for project overview and directory structure.

- Run `make help` to discover available targets.
- After making changes, run `make all`, `make tidy`, and `make format`.
  Do not run build outputs directly — always use make targets.
